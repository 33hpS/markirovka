# Application Environment Variables (copy to .env or .env.local and fill values)

# ----------------------------------------------------------------------------
# Supabase (Frontend: used by Vite build)
# ----------------------------------------------------------------------------
# Project URL (Settings → API → URL)
VITE_SUPABASE_URL=
# Anon Key (Settings → API → anon public)
VITE_SUPABASE_ANON_KEY=

# ----------------------------------------------------------------------------
# Cloudflare R2 (Frontend usage only for non-sensitive values)
# For secure uploads use Worker binding and server-side signing.
# ----------------------------------------------------------------------------
# S3-compatible endpoint for your account/region
VITE_R2_ENDPOINT=
# Access key id (avoid exposing on frontend in production; prefer Worker)
VITE_R2_ACCESS_KEY_ID=
# Secret access key (do NOT commit real value; prefer Worker)
VITE_R2_SECRET_ACCESS_KEY=
# Bucket name
VITE_R2_BUCKET_NAME=
# Optional region (R2 uses "auto" by default)
VITE_R2_REGION=auto

# ----------------------------------------------------------------------------
# App Settings
# ----------------------------------------------------------------------------
# Optional custom API URL (if you add Worker API routes)
VITE_API_URL=

# ----------------------------------------------------------------------------
# Notes:
# - Do NOT commit real secrets. This file is an example template.
# - For Cloudflare Workers, set secrets via:
#     npx wrangler secret put VITE_SUPABASE_URL
#     npx wrangler secret put VITE_SUPABASE_ANON_KEY
#     npx wrangler secret put VITE_R2_ENDPOINT
#     npx wrangler secret put VITE_R2_ACCESS_KEY_ID
#     npx wrangler secret put VITE_R2_SECRET_ACCESS_KEY
#     npx wrangler secret put VITE_R2_BUCKET_NAME
# - Consider using Worker R2 binding instead of exposing R2 keys to the browser.
# Environment Variables Template
# Copy this file to .env.local and fill in your values

# API Configuration
VITE_API_BASE_URL=http://localhost:8000/api
VITE_APP_TITLE="Маркировочная система"

# Service URLs
VITE_PRINT_SERVICE_URL=http://localhost:8001
VITE_QR_SERVICE_URL=http://localhost:8002

# File Upload Configuration
VITE_MAX_FILE_SIZE=10485760
VITE_ALLOWED_FILE_TYPES=image/png,image/jpeg,application/pdf

# Supabase (REQUIRED for Realtime sync)
# Get these from: https://supabase.com/dashboard/project/wjclhytzewfcalyybhab
# Settings → API → URL and anon/public key
VITE_SUPABASE_URL=https://wjclhytzewfcalyybhab.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Cloudflare R2 (optional)
# Uncomment to enable Cloudflare R2 uploads
# VITE_R2_ENDPOINT=https://<account>.r2.cloudflarestorage.com
# VITE_R2_ACCESS_KEY_ID=<access-key-id>
# VITE_R2_SECRET_ACCESS_KEY=<secret-access-key>
# VITE_R2_BUCKET_NAME=<bucket-name>

# Development Configuration (optional)
# VITE_DEBUG=true